# Install

```bash
pip install -e .
# install Tailwind CLI (required for CSS):
npm install -g tailwindcss
# or per-project:
npm install -D tailwindcss
```
Requires Python 3.10+. Tailwind CLI should be on PATH for CSS builds.

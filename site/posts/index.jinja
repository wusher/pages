<h1 class="text-3xl font-bold mb-6">Posts</h1>
<p class="text-slate-600 mb-8">Latest writing from your site.</p>

<div class="space-y-8">
{% for post in pages.group("posts").published().sorted() if post.slug != "index" %}
  <article class="border-b border-slate-200 pb-6">
    <h2 class="text-xl font-semibold mb-2">
      <a href="{{ post.url }}" class="text-teal-700 hover:text-teal-900">{{ post.title }}</a>
    </h2>
    <p class="text-sm text-slate-500 mb-2">{{ post.date.strftime('%B %d, %Y') }}</p>
    <p class="text-slate-700">{{ post.description }}</p>
  </article>
{% else %}
  <p class="text-slate-600">No posts yet. Add markdown files in <code>site/posts/</code>.</p>
{% endfor %}
</div>
